interface FilterLayoutProps {
    includes: string[];
    excludes: string[];
}

export default function FilterLayout({includes, excludes}: FilterLayoutProps) {
    return (
        <div>
        </div>
    )
}